# Release-only secrets for local/manual release work.
# The app itself does not load this file at runtime.
# Keep the real `.env.release.local` gitignored.

# Tauri updater signing key (private)
TAURI_SIGNING_PRIVATE_KEY=

# Password protecting the private key above
TAURI_SIGNING_PRIVATE_KEY_PASSWORD=
