{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for Immich Beam",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "opener:allow-reveal-item-in-dir",
    "autostart:default",
    "dialog:default",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
